# Anagram grops

## Question - Medium 

Given an array of strings strs, group all anagrams together into sublists. you may retrun the ouptu in any order.

An anagram is a string that contains the exact same characters as another strings, but the odrder of the characters can be different.


### Example 1:

```console

Input : strs = [ "act", "pots", "tops", "cat" , "stop" , "hat"]
Output: [["hat] , ["act", "cat"],["stop","pots","tops"]]
```

### Example 2:

```console

Input: strs = ["x"]
Output: [["x"]]

```

### Example 3:

```console 
Input: strs = [""]

Output: [[""]]
```

Constraints:

1. 